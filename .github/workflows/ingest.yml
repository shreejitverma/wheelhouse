name: ingest

# Fast path: a source repo (or a manual test) hands us an item that needs the
# maintainer's decision, and we create, safely reuse, refresh, or
# activity-reflect a decision-card issue for it.
# For eligible PR-review or issue-triage cards, this also queues the advisory
# auto-triage workflow after writing the per-revision cache marker.
# A brand-new eligible card is created HELD (`pending-triage`, no decision
# checkboxes) before that first attempt, then published when the attempt
# completes or fails open.
# This path trusts the explicit dispatch identity hint, then independently
# observes PR facts and applies the same source-permission policy as the scan.
#
# Portability: the owner is github.repository_owner; the fleet + policy come from
# wheelhouse.config.yml. Nothing here is account-specific.

on:
  repository_dispatch:
    # `wheelhouse-item` is the current dispatch event type. `triage-item` is the
    # legacy alias kept for back-compat: source repos onboarded before the rename
    # still send it, so we keep listening for both rather than silently breaking
    # their live dispatchers. New source-repo workflows should send wheelhouse-item.
    types: [wheelhouse-item, triage-item]
  workflow_dispatch:
    inputs:
      repo:
        description: "Target repo name (under your owner), e.g. my-service"
        required: true
      number:
        description: "Target PR/issue number"
        required: true
      kind:
        description: "pr-review | ci-approval | issue-triage"
        default: pr-review
      head_sha:
        description: "PR head SHA (recommended for pr-review/ci-approval)"
        required: false
      updated_at:
        description: "Target PR/issue updatedAt (recommended; issue-triage uses it as the triage revision)"
        required: false
      title:
        description: "Short title of the target"
        required: false
      summary:
        description: "One-line situation summary"
        required: false
      priority:
        description: "high | med | low"
        default: med
      options:
        description: "Comma-separated checkbox option keys (optional)"
        required: false

permissions:
  contents: read
  issues: write
  actions: write

# Share the global card-lifecycle queue with scan-backstop and the decision
# handler. A closed-card reopen or new-card create must not race another path
# into two open cards for one target.
concurrency:
  group: wheelhouse-backstop
  queue: max
  cancel-in-progress: false

jobs:
  ingest:
    # Owner-gate where applicable: repository_dispatch is already token-gated
    # (only a writer's token can send it); the manual path must be the owner.
    if: github.event_name == 'repository_dispatch' || github.actor == github.repository_owner
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-python@v5
        with:
          python-version: "3.x"

      - name: Install deps
        run: pip install --quiet pyyaml

      - name: Normalize payload -> item.json
        env:
          GITHUB_REPOSITORY_OWNER: ${{ github.repository_owner }}
          PAYLOAD: ${{ toJSON(github.event.client_payload) }}
          INPUT_REPO: ${{ github.event.inputs.repo }}
          INPUT_NUMBER: ${{ github.event.inputs.number }}
          INPUT_KIND: ${{ github.event.inputs.kind }}
          INPUT_HEAD_SHA: ${{ github.event.inputs.head_sha }}
          INPUT_UPDATED_AT: ${{ github.event.inputs.updated_at }}
          INPUT_TITLE: ${{ github.event.inputs.title }}
          INPUT_SUMMARY: ${{ github.event.inputs.summary }}
          INPUT_PRIORITY: ${{ github.event.inputs.priority }}
          INPUT_OPTIONS: ${{ github.event.inputs.options }}
        run: python scripts/build_item.py item.json

      - id: source-policy
        name: Route exact source-permission policy
        env:
          GH_TOKEN: ${{ secrets.FLEET_TOKEN }}
          GITHUB_REPOSITORY_OWNER: ${{ github.repository_owner }}
        run: python scripts/maintainer_edits_policy.py route-item item.json policy-notices.json

      - name: Post exact source-permission notice
        if: steps.source-policy.outputs.admitted != 'true'
        env:
          GH_TOKEN: ${{ secrets.FLEET_TOKEN }}
          GITHUB_REPOSITORY_OWNER: ${{ github.repository_owner }}
        run: |
          python scripts/maintainer_edits_policy.py notify policy-notices.json policy-notice-results.json
          python scripts/maintainer_edits_policy.py attach-item item.json policy-notice-results.json

      # A dispatch is only an invalidation/identity hint. Ordinary PR-review
      # facts are replaced by an exact ReviewObservation from the same reducer
      # used by the scheduled scan before any card projection.
      - name: Observe the exact PR revision
        if: steps.source-policy.outputs.admitted == 'true'
        env:
          GH_TOKEN: ${{ secrets.FLEET_TOKEN }}
          GITHUB_REPOSITORY_OWNER: ${{ github.repository_owner }}
        run: python scripts/projection_writer.py observe-item item.json

      - id: upsert
        name: Create, reuse, or refresh the decision card
        env:
          # Cards live in THIS repo: the default token is correct, and using it
          # keeps card activity from re-triggering the decision handler.
          GH_TOKEN: ${{ github.token }}
          GITHUB_REPOSITORY_OWNER: ${{ github.repository_owner }}
          # Same signal the "Queue auto triage" step below gates on: a
          # brand-new eligible card is created HELD only when triage will
          # actually be queued for it (see AGENTS.md "Held cards").
          WHEELHOUSE_AUTO_TRIAGE_HAS_TOKEN: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN != '' }}
        run: python scripts/render_card.py upsert --item-file item.json

      - name: Prepare source-policy close handoff
        if: steps.source-policy.outputs.admitted != 'true'
        env:
          GH_TOKEN: ${{ github.token }}
          GITHUB_REPOSITORY_OWNER: ${{ github.repository_owner }}
        run: python scripts/maintainer_edits_policy.py prepare-close item.json "${{ steps.upsert.outputs.issue }}" policy-actions.json

      - name: Close verified source-policy target
        if: steps.source-policy.outputs.admitted != 'true'
        env:
          GH_TOKEN: ${{ secrets.FLEET_TOKEN }}
          GITHUB_REPOSITORY_OWNER: ${{ github.repository_owner }}
        run: python scripts/maintainer_edits_policy.py close policy-actions.json policy-close-results.json

      - name: Record source-policy outcome
        if: steps.source-policy.outputs.admitted != 'true'
        env:
          GH_TOKEN: ${{ github.token }}
          GITHUB_REPOSITORY_OWNER: ${{ github.repository_owner }}
        run: python scripts/maintainer_edits_policy.py record policy-close-results.json

      - id: auto-triage-gate
        env:
          HAS_TOKEN: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN != '' }}
        run: echo "has_token=$HAS_TOKEN" >> "$GITHUB_OUTPUT"

      - name: Queue auto triage
        if: steps.auto-triage-gate.outputs.has_token == 'true' && steps.source-policy.outputs.admitted == 'true'
        env:
          GH_TOKEN: ${{ github.token }}
          WHEELHOUSE_FLEET_TOKEN: ${{ secrets.FLEET_TOKEN }}
          WHEELHOUSE_AUTOMERGE_HAS_TOKEN: ${{ secrets.CLAUDE_CODE_OAUTH_TOKEN != '' }}
          GITHUB_REPOSITORY_OWNER: ${{ github.repository_owner }}
        # --issue threads the number `upsert` just created/refreshed, so this
        # reads the card back by number (read-after-write consistent) instead
        # of relying on find_card's label-filtered listing, which can race a
        # just-created issue. Falls back to find_card if the output is empty.
        run: python scripts/render_card.py queue-triage --item-file item.json --issue "${{ steps.upsert.outputs.issue }}"
